// The starter ships a PostgreSQL-backed Drizzle setup. Schemas for MySQL and
// SQLite also exist under ./drizzle/schema and can be swapped in by updating
// this barrel plus `drizzle.config.ts` and `drizzle/client.ts`.
export * from "./drizzle";
